**Status: CANDIDATE. Declarative addition proof: reuses the registered discriminator_table capability — zero consumer edits. Promotion via TP-CURR packets under Sol/owner control.**

# Procedure (model-facing)

1. Receive the declared `final_letter` + `shadda_on_yaa_evidence` (+, for
the none_word_final_yaa branch, `base_category_evidence`) features.
2. Run the discriminator table: `taa_marbuta` + `written_shadda` ->
abstract_noun_yaa (no rival collides on this branch). `none_word_final_yaa`
+ `written_shadda` collides between TWO rivals sharing the exact same
shape -- a genuine nisba (مِصْرِيّ) and a non-nisba faʿīl-pattern adjective
(قَوِيّ) -- so shape alone is never enough: `base_category_evidence:
nisba_relational_attested` -> nisba_relational_yaa;
`base_category_evidence: non_nisba_adjective_attested` ->
non_nisba_geminated_yaa_adjective; missing or unrecognised base-category
evidence -> abstain, never a default nisba pick. 3. Missing/unmarked
shadda evidence, a final letter outside the declared classes, or an
ordinary taa-marbuta noun with no geminated yaa at all -> abstain
`insufficient_features`. 4. Never treat normalisation or the mere presence
of a taa marbuta as confirmation of the shadda; the mark must be written.
