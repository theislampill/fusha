**Status: CANDIDATE. Declarative addition proof: reuses the registered discriminator_table capability — zero consumer edits. Promotion via TP-CURR packets under Sol/owner control.**

# Procedure (model-facing)

1. Receive the declared `final_letter` + `shadda_on_yaa_evidence` features.
2. Run the discriminator table: `none_word_final_yaa` + `written_shadda` ->
nisba_relational_yaa; `taa_marbuta` + `written_shadda` -> abstract_noun_yaa.
3. Missing/unmarked shadda evidence, a final letter outside the two
declared classes (e.g. an intensive adjective's geminated yaa with no
licensed final-letter class), or an ordinary taa-marbuta noun with no
geminated yaa at all -> abstain `insufficient_features`. 4. Never treat
normalisation or the mere presence of a taa marbuta as confirmation of the
shadda; the mark must be written.
