**Status: CANDIDATE. Outside `sarf/`/`nahw/` by design; nothing here is released or certified; promotion routes through the TP-CURR packets under Sol/owner control. Machine pack read by the NON-AUTHORITATIVE fixture harness `tools/curriculum_unit_consumer.py`; every non-abstention outcome is `candidate_pending`.**

# Procedure (model-facing)

1. Require supplied root radicals (evidence ladder) -> `no_root_evidence`.
2. Require the supplied letters to be exactly the bare letters of the
written surface -> `surface_letter_mismatch`. 3. Test the فُعَيْل shape
against the letters with radicals in R-slots and the literal ي in its
template position -> zero/multiple survivors abstain `no_template`/
`ambiguous_template`. 4. Shape alone never certifies identity or meaning:
require attested `base_lexeme_evidence` (else
`base_lexeme_evidence_required`) AND a licensed `diminutive_affix_evidence`
bound to the ي marker (else `diminutive_affix_evidence_required`) — an
explicitly unattested/unlicensed claim still abstains, never a harmless
extra. 5. If the root evidence declares a weak position/radical, require the
exact bound declaration (`weak_declaration_unbound` if absent/mismatched)
AND a licence for that template+slot+radical in
`weak_realizations.by_template` (`weak_realization_unlicensed` otherwise) —
nothing is licensed by default. 6. Emit class + template + evidence as a
CANDIDATE proposal; never a lexeme, sense, translation or occurrence fact.
