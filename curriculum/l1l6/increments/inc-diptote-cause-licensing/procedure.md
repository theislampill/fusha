**Status: CANDIDATE. Outside `sarf/`/`nahw/` by design; nothing here is released or certified; promotion routes through the TP-CURR packets under Sol/owner control. Machine pack consumed by `tools/curriculum_unit_consumer.py`.**

# Procedure (model-facing)

1. Read the declared triggers: the semantic member, the formal member, and
independently the template-family membership. 2. Match the configuration
against the licensing table. 3. Licensed -> emit the outcome, including the
explicit fully-declining outcome where only one feature is present. 4. Test
definiteness and construct headship before any register question. 5. Apply the
verse override only inside verse and only with identified metrical or rhyme
pressure; emit it attributed. 6. An unlisted template, or unstated triggers ->
reject_reconstruction. 7. Nothing here certifies.
