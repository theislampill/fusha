**Status: CANDIDATE. Declarative addition proof: reuses the registered discriminator_table capability — zero consumer edits. Promotion via TP-CURR packets under Sol/owner control.**

# Procedure (model-facing)

1. Receive the declared `particle` + `verb_tense` features. 2. Run the
discriminator table: match particle AND tense together against the closed
registry rows. 3. One survivor -> the candidate class label only (never a
mood/case/governor key); zero or a particle outside the registry (including
a genuinely governing one like لن/لم) -> abstain `insufficient_features`.
4. Never license قد without a declared verb tense — the same particle
splits into two distinct, still nongoverning readings depending on tense.
