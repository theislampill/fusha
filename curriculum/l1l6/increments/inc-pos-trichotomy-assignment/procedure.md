**Status: CANDIDATE. Outside `sarf/`/`nahw/` by design; nothing here is released or certified; promotion routes through the TP-CURR packets under Sol/owner control. Machine pack consumed by `tools/curriculum_unit_consumer.py`.**

# Procedure (model-facing)

1. Collect the SUPPLIED marker evidence for the token in its licensing frame
(nominal marker, verbal marker, whether all markers were rejected, whether the
word can stand as an answer). 2. Test the three functions. 3. One survivor ->
candidate class; two survivors -> preserve both readings; none -> abstain
insufficient_features. 4. Never assign a class from meaning, from translation
category, or from word size. 5. Output feeds the hover class panel as a
CANDIDATE row and is consumed downstream by the ownership and case units,
which presuppose a class they do not themselves produce.
