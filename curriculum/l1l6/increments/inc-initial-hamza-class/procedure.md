**Status: CANDIDATE. Declarative addition proof: reuses the registered discriminator_table capability — zero consumer edits. Promotion via TP-CURR packets under Sol/owner control.**

# Procedure (model-facing)

1. Receive the declared initial-seat class + vocalization-evidence features
(never inferred from a normalized/collapsed key). 2. Run the discriminator
table: `hamza_qat_alif` + `written_mark` -> hamzat_qat;
`hamza_wasl_seated` + `written_mark` -> hamzat_wasl. 3. Any other seat
(including a bare alif with no hamza mark), or vocalization evidence that is
not a written mark, survives no function -> abstain
`insufficient_features`. 4. Never guess a class from an unmarked seat, and
never let asserted evidence over a non-hamza seat license either class.
