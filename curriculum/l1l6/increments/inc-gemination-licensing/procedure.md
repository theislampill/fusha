**Status: CANDIDATE. Outside `sarf/`/`nahw/` by design; nothing here is released or certified; promotion routes through the TP-CURR packets under Sol/owner control. Machine pack read by the NON-AUTHORITATIVE fixture harness `tools/curriculum_unit_consumer.py`; every non-abstention outcome is `candidate_pending`.**

# Procedure (model-facing)

1. Require supplied root radicals (evidence ladder) -> `no_root_evidence`.
2. Require the supplied letters to be exactly the bare letters of the
written surface -> `surface_letter_mismatch`. 3. Test each template's exact
shape against the letters with radicals in R-slots and the GEM slot matched
like an ordinary radical position -> zero survivors abstain `no_template`.
4. Only THEN check the gemination licence, separately: require a bound
declaration (slot + radical) -> `gemination_declaration_unbound`; require a
WRITTEN shadda source, never assumed -> `shadda_source_unresolved`; require
a non-jussive mood -> `jussive_variant_unlicensed`; require a word-internal
(never cross-boundary) merger -> `cross_boundary_merger_unlicensed`. 5. Even
with an exact declared environment, require the pack to explicitly license
THIS slot+radical combination in `gemination_realizations.by_template` ->
`gemination_realization_unlicensed` otherwise. 6. Emit class + template as a
CANDIDATE proposal; nothing here derives the un-geminated jussive form.
