**Status: CANDIDATE. Outside `sarf/`/`nahw/` by design; nothing here is released or certified; promotion routes through the TP-CURR packets under Sol/owner control. Machine pack read by the NON-AUTHORITATIVE fixture harness `tools/curriculum_unit_consumer.py`; every non-abstention outcome is `candidate_pending`.**

# Procedure (model-facing)

1. Require supplied root radicals (evidence ladder) -> `no_root_evidence`.
2. Require the supplied letters to be exactly the bare letters of the
written surface -> `surface_letter_mismatch`. 3. Test each template's exact
shape against the letters with radicals in R-slots and the GEM slot matched
like an ordinary radical position -> zero survivors abstain `no_template`.
4. Only THEN check the gemination licence, separately and IN ORDER: the
pack's own declared merge slot must resolve to a real R-slot that matches
THIS exact template's own shape -> `gemination_slot_declaration_malformed`
otherwise; require a bound declaration (slot + radical) ->
`gemination_declaration_unbound`; require a WRITTEN shadda source, never
assumed -> `shadda_source_unresolved`; require that shadda to be VERIFIED
against the actual written surface at the GEM slot's own letter position ->
`shadda_not_in_surface`. 5. Even with an exact declared environment, require
the pack to explicitly license THIS exact template/slot against the
evidence's attested basis in `gemination_realizations.by_template` ->
`gemination_realization_unlicensed` otherwise. 6. Mood
(indicative/subjunctive/jussive) and cross-clitic-boundary idgham are never
consulted: both templates this pack declares are mood-invariant (a
perfect-tense verb carries no mood inflection at all; a participle is a
noun) and neither declares a clitic layer a merger could cross -- the
geminate-ROOT jussive alternation (a genuinely doubled root losing its
written doubling in the jussive, e.g. لم يَمْدُدْ from يَمُدُّ) is a
DIFFERENT phenomenon over a different root/template family this pack does
not declare; conflating it with Form II's inherent, mood-invariant
gemination was the recorded defect this pack now avoids. 7. Emit class +
template as a CANDIDATE proposal; nothing here derives an un-geminated
variant, resolves voice, or distinguishes participle from place/time noun.
