**Status: CANDIDATE. Declarative addition proof: reuses the registered discriminator_table capability — zero consumer edits. Promotion via TP-CURR packets under Sol/owner control.**

# Reference — lexically feminine noun registry

| Function | Closed registry |
|---|---|
| lexically_feminine_noun | أرض، شمس، ريح، نار، حرب |

A CLOSED declared registry of nouns that are lexically feminine despite
carrying none of the regular feminine markers (ة/اء/ى) — membership is
exact, never inferred from shape. A noun outside the registry never gets a
shape-based vote here, whether it is masculine (قمر) or already regularly
feminine-marked by ة (a DIFFERENT, morphological mechanism this pack does
not decide). Growing the registry is a deliberate, reviewed pack edit —
never a runtime inference.
