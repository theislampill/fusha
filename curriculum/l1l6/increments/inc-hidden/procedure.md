**Status: CANDIDATE. Outside `sarf/`/`nahw/` by design; nothing here is released or certified; promotion routes through the TP-CURR packets under Sol/owner control. Machine pack consumed by `tools/curriculum_unit_consumer.py`.**

# Procedure (model-facing)

1. Match the SUPPLIED construction type against the licensing table (no
free-text inference). 2. Licensed -> emit the typed hidden node with
obligatoriness + attribution flags. 3. Unlicensed -> reject_reconstruction.
4. Rivals -> all alternatives, none preferred. 5. Nothing certifies; output
is candidate analysis for the two-vote/scholar planes.
