**Status: CANDIDATE. Outside `sarf/`/`nahw/` by design; nothing here is released or certified; promotion routes through the TP-CURR packets under Sol/owner control. Machine pack consumed by `tools/curriculum_unit_consumer.py`.**

# Reference — hidden and deleted structure (taqdir)

Licensed reconstructions (closed table): mustatir pronoun of the imperative
(obligatory) and of 3rd-person verbs (optional); the vocative's deleted call
verb (analysis-dependent); the elided 'a'id of relative object gaps; elided
clauses in answer fragments. Everything else: reject. A hidden element is a
TYPED node (`is_reconstruction=true`), displayed as analysis, never as text.
Rival reconstructions all survive with attribution. This is the capability
the repo lacked (crosswalk xn-08); it enters as candidate machinery only.
