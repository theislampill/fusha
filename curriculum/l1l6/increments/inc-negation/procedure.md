**Status: CANDIDATE. Declarative addition proof: reuses the registered discriminator_table capability — zero consumer edits. Promotion via TP-CURR packets under Sol/owner control.**

# Procedure (model-facing)

1. Receive the resolved particle (homograph guard upstream) + declared
context features. 2. Run the discriminator table. 3. One survivor ->
candidate function with its tense/mood effect; several -> preserve
alternatives; none -> abstain. 4. Never emit the bare verb's surface tense
under a governing negative.
