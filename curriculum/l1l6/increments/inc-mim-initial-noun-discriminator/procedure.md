**Status: CANDIDATE. Outside `sarf/`/`nahw/` by design; nothing here is released or certified; promotion routes through the TP-CURR packets under Sol/owner control. Machine pack consumed by `tools/curriculum_unit_consumer.py`.**

# Procedure (model-facing)

1. Collect SUPPLIED evidence: prefix vowel, internal vowel before the last
radical, final ending, form class of the source verb, whether the token is
vocalized. 2. Test all five classes. 3. One survivor -> candidate class;
several -> preserve alternatives; none -> abstain insufficient_features.
4. Never default an unpointed mim-initial token to the place class. 5. Never
predict an instrument template for a root; classify only what is attested.
