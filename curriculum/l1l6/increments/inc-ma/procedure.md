**Status: CANDIDATE. Outside `sarf/`/`nahw/` by design; nothing here is released or certified; promotion routes through the TP-CURR packets under Sol/owner control. Machine pack consumed by `tools/curriculum_unit_consumer.py`.**

# Procedure (model-facing)

1. Collect SUPPLIED context features (following form, clause type, antecedent
slot, mood pattern, attachment, position). 2. Test all seven functions'
discriminators. 3. One survivor -> candidate function; several -> preserve
alternatives; none -> abstain insufficient_features. 4. Never emit government
claims; never default. 5. Output feeds hover's function panel + the p099
occurrence lane as CANDIDATE rows.
