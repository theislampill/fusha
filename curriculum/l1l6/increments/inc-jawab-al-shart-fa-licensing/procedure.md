**Status: CANDIDATE. Outside `sarf/`/`nahw/` by design; nothing here is released or certified; promotion routes through the TP-CURR packets under Sol/owner control. Machine pack consumed by `tools/curriculum_unit_consumer.py`.**

# Procedure (model-facing)

1. Locate the response boundary and read the class of its leftmost element
from the declared inputs. 2. Match that class against the licensing table.
3. Licensed -> emit connective_required or connective_forbidden with the
operator that assigns the mood. 4. Attributed rows carry their
analysis-dependence and never certify. 5. An unclassified or unlisted opener
-> reject_reconstruction; the inventory is not exhaustive and the pack does
not close it by assimilation. 6. Nothing here certifies.
