**Status: CANDIDATE. The pedagogical_projection capability: a REAL data-driven consumer for instructional units (tutor/drill/hover surfaces). Registered in tools/curriculum_unit_consumer.py; packs and projections are recompute-gated.**

# Reference — pedagogical projection contract

One qualified unit record → one candidate teaching artifact with:
prerequisite statement, beginner/intermediate/advanced explanations,
technical identity (the verbatim unit fact reference), recognition cue,
common-error warning, contrast/transfer example, unresolved/rival notice and
a tutor/drill/hover destination. Required inputs missing → abstention (no
invented content). The emission set is contract data: dropping the
error-warning emission is the recorded v0 defect the pedagogy loop keeps red.
