**Status: CANDIDATE. Outside `sarf/`/`nahw/` by design; nothing here is released or certified; promotion routes through the TP-CURR packets under Sol/owner control. Machine pack read by the NON-AUTHORITATIVE fixture harness `tools/curriculum_unit_consumer.py`; every non-abstention outcome is `candidate_pending`.**

# Procedure (model-facing)

1. Require supplied root radicals (evidence ladder) -> `no_root_evidence`.
2. Require the supplied letters to be exactly the bare letters of the
written surface -> `surface_letter_mismatch`. 3. Require exactly FOUR
declared radicals; a three- or five-radical root never borrows or over-fills
a slot -> `no_template`. 4. Test each of the three declared shapes (bare
perfect; مُـ + active participle; مُـ + passive participle) against the
letters with radicals in R1-R4 -> zero survivors abstain `no_template`.
5. If BOTH the active and passive مُـ-participle rows survive (letter-
identical once the radicals stand literally), abstain `ambiguous_template`
with both ids as alternatives — never silently pick one. 6. Otherwise emit
class + template as a CANDIDATE proposal; nothing here infers voice or
mood.
