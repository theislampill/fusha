**Status: CANDIDATE. Outside `sarf/`/`nahw/` by design; nothing here is released or certified; promotion routes through the TP-CURR packets under Sol/owner control. Machine pack consumed by `tools/curriculum_unit_consumer.py`.**

# Procedure (model-facing)

1. Collect SUPPLIED evidence: particle identity, position relative to the two
terms, case identity, polarity of the preceding clause, clause type, size of
the following unit, gemination, presence of a paired second member. 2. Test
every function, coordinating and non-coordinating alike. 3. One survivor ->
candidate function; several -> preserve alternatives with attribution; none ->
abstain insufficient_features. 4. Never emit a coordination claim from
particle identity alone. 5. Output feeds the hover coordination panel as a
CANDIDATE row.
