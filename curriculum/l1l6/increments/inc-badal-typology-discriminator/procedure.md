**Status: CANDIDATE. Outside `sarf/`/`nahw/` by design; nothing here is released or certified; promotion routes through the TP-CURR packets under Sol/owner control. Machine pack consumed by `tools/curriculum_unit_consumer.py`.**

# Procedure (model-facing)

1. Confirm SUPPLIED case identity and the absence of a coordinator. 2. Collect
the remaining supplied evidence: resumptive clitic, its antecedent,
referential identity, the part-versus-containment judgement, the status of the
first nominal, whether a property is predicated. 3. Test the five rows. 4. One
survivor -> candidate type; several -> return them together, which for the
total pair is the recorded shared-label outcome; none -> abstain
insufficient_features. 5. Never invent a clitic and never route a descriptive
follower through this table.
