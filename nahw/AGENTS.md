# nahw/ — agent stub

Syntax/context skill and evals — how sentence context changes the correct gloss. Read
`SKILL.md` here BEFORE any context-sensitive decision; root rules in the top-level `AGENTS.md`.

- Grammar-safety gate: general LLM confidence is not evidence; a correct answer with wrong
  iʿrāb reasoning is unsafe (`evals/grammar-decision-gates.json`).
- Released: @2. Candidate rules (@2.1–@2.4) are guard-gated pending owner adjudication.
- Genuine disputes route to arbitration/scholar packets (`docs/blockers.yaml`), never to a vote.
