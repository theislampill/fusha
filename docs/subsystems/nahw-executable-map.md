# Nahw executable map (as-built audit)

Verified against: commit `637d7da` (origin/main), audited 2026-07-29 by direct file reads and greps in this worktree. Claims cited `file:line`. Implemented behavior only; intended-but-not-executable items are GAP-marked in §8 with work-packet stubs.

**Update 2026-07-30 (Burst A2 — naḥw consumer and behavioural-gate wiring; repair rounds 1–8). Integrated provenance: branch base `6536c31`, rebased onto the A1-merged main `4d13fd2` (PR #135), serialized harness integration applied on top, then repaired in place through round 8.** The A2 gates are now executed by `tools/check_regressions.py` itself: the three consumer self-tests, the behavioural/mutation suite and the whole eval runner run as subprocesses, the runner's `--json` result is parsed as exactly one document, and the measured counts are asserted.

**Update 2026-08-03 (occurrence-specific contextual مَا relative-vs-negation; wave-1 repair round).** An 8th `particle-context-rules.json` rule, `maa_relative_vs_negation`, discriminates مَا by an occurrence-specific CONTEXTUAL FRAME rather than a content-letter diacritic (مَا carries none): `tools/fusha_nahw_particle_rules.maa_context_frame()` reads the rule's own `frame_table` — six frames uniquely bound to `relative` or `negation`, plus one intentionally non-unique frame (`not_object_before_other`) that preserves both — and a separate `recognized_unmapped_frames` list (`object_of_verb_then_other`) names a frame the source mechanism can genuinely report but that has deliberately NOT been generalised from the mapped `object_of_verb_then_*` rows, so it stays pending (`frame_row_missing`) rather than inventing a conclusion. A duplicate `frame_table` key fails the WHOLE rule closed (`frame_table_duplicate_key`) rather than letting the first match silently shadow a later one. Every non-candidate outcome now carries the REAL `evidence_defect` at the top level — including through `negation_effect()`'s `maa_context_frame` context path, which used to leave a stale `absent` on an ambiguous, replayed or off-vocabulary frame — and an out-of-scope (`relative`-routed-through-negation) result's rivals are recomputed with `selected=null` rather than leaking the pre-rejection `candidate_selected`. The new bank `nahw/evals/ma-function-occurrence-eval.jsonl` (6 rows) is exercised by `tools/run_nahw_evals.py --bank ma-function-occurrence`, the DEFAULT `--bank all` / no-flag route, and `--self-test`, and is registered `implemented_and_consumed` in both `A2_ARTIFACT_OWNERSHIP` and `tools/fusha_eval_coverage.py`'s own independent allowlist (every row's exact decision, function/rival set and defect is checked against the real consumer; no row is quarantined). This raises the runner's committed totals to **7 execution groups** deciding **8 physical eval artifacts** totalling **320 rows**, of which **19 rows now earn behavioural credit** — `public-boundary-scanner-eval.jsonl` (10), `largelexicon-function-collision-safety.jsonl` (3) and `ma-function-occurrence-eval.jsonl` (6) — with **21 rows still quarantined** (9 state-machine + 12 hover-context) and the remaining 5 artifacts still `fixture_only`. Candidate-only posture is unchanged: a validated contextual-frame observation reaches `candidate` at `two_vote_required`, never `resolved` or certified.

**Update 2026-08-03 (Train B review-repair round, blocker + 7 important findings).** An independent adversarial
review found the مَا frame table's `unique` flag was read as "this frame determines the occurrence's function",
when the table only ever discriminates the relative-vs-negation AXIS (B1, blocker): the table now declares
`axis: [relative, negation]` and `functions_not_discriminated` explicitly, every row is `unique_within_axis`
(renamed from `unique`), and `maa_context_frame()`/`negation_effect()`/`rival_analyses()` carry the five
out-of-axis functions (interrogative, maṣdariyya, temporal, preventive_kaffa, laysa_like) as permanently
`unresolved` with `defeater=not_examined_by_this_axis` — an axis-derived candidate now rejects at most its
in-axis sibling, never a function the frame never examined. `tools/run_nahw_evals.py` now also checks every
`ma-occ-*` row's `(quran_loc, word, surface)` against `qamus/indexes/quran-loc-surface/index.jsonl` — the same
authority the governor banks use — before trusting the row's own claim (I1). `tools/fusha_governor.py`'s claim
lint is now head-aware for ʿāmil kind (a genitive claimed "by a preposition" is refused when the real head is a
ẓarf/iḍāfa noun; F11 now carries a real `claims[]` entry exercising this on the ordinary path, I2); the
coordination case-following sentinel now runs the SAME released `mood_basis` gate as fixed-tuple reason keys, so
candidate `nahw@2.4` basis `tajarrud` stays refused there too (I3); the nawāsikh khabar sweep now stops at a
genitive/oblique (iḍāfa/ṣifa) boundary instead of fabricating a contradiction against a role the token was never
filling (F14 pins the 7:56-shaped إِنَّ رَحْمَتَ ٱللَّهِ frame, I4); the bare noun+noun iḍāfa-candidate fallback
no longer fires when the first noun immediately follows a verb — F8's ٱللَّهُ/نَفْسًا (fāʿil + mafʿūl bihi) no
longer acquires a false `idafa_dependent` edge (I5); and the three Train B governor banks' BUILT lattices (not
just the committed 6-row sample) now also clear `validate_dependency_lattice.py --verify-occurrence-identity` in
`check_regressions.py`'s own harness block, which surfaced and fixed two small pre-existing defects (a headless
hidden-ism edge carrying a self-contradictory `candidate_head`; a ẓanna-family sense-gated abstention missing
`governing_regime`) that `--run-bank` alone could never have caught (I6). The `maa_context_frame()` on/off probe
in `validate_nahw_skill.py` is reworded from "load-bearing in a production path" to a callable-level, data-driven
probe — its only callers are the eval runner, this validator and the behavioural gates, exactly the standard
`negation-rules.json` is held to (I7). Minor: the four Train B refusal-control rows now declare
`source_unit.scope=arbitrary` rather than the false `in_scope_source_addressed`; N1/N2/N4's refusal is now
asserted against their OWN declared mechanism (ayah-absent-from-spine, a coverage accident) rather than merely
"some error", so a future spine change turns the test red for the right reason instead of silently staying green
for the wrong one; the coordination/nawāsikh procedure docs no longer cite unimplemented abstention-reason
values (`head_not_supplied`, `head_bears_mahall`, `licenser_unknown`); and the مَا frame table now discloses its
own author-circularity (frame and expected function share an author, from no mechanical source).

Those pre-existing counts, kept distinct on purpose: **6 of the 7 execution groups** (the ones present before this round) decide **7 of the 8 physical eval artifacts** (the `function-polysemy` group decides two, `particle-function-eval.jsonl` and `irab-polysemy-eval.jsonl`) totalling **314 of the 320 rows**; of the 13 rows credited among those, `public-boundary-scanner-eval.jsonl` (10) and `largelexicon-function-collision-safety.jsonl` (3) were the only two artifacts declared `implemented_and_consumed` before `ma-function-occurrence-eval.jsonl` joined them; **21 rows are quarantined** (9 state-machine + 12 hover-context); the remaining **5 artifacts are `fixture_only`**; hover bound 8 and mutations 5. The figure `82` is **consumer-invocation events with mixed denominators** (identity + ablation + mutation + routing + scanner + homograph + occurrence-decision + occurrence-replay, omitting the hover base comparisons) — it is not a row-coverage number and is never compared with 320 or 19. `tools/fusha_eval_coverage.py` registers A2 through A1's invoked-runner/callable-result architecture only — never from a filename, source text, a declaration or a failed run — and validates the returned result against the reporter's OWN artifact-to-consumer allowlist and its OWN independently counted row denominators, so the fixture-only artifacts and all 21 quarantined rows stay visibly uncovered even if the runner claimed otherwise. **GAP-N2 through GAP-N5 remain PARTIAL, not closed**; 5 of 12 rules files are load-bearing and 7 are fixture-gated; the five A2 packets carry every remaining gap; nothing is certified and no candidate rule is promoted.

**Lane detail (unchanged).** GAP-N2, GAP-N3, GAP-N4 and GAP-N5 are **PARTIAL, not closed**; §2, §6 and §8 below carry the measured post-A2 state. The new surfaces are three bounded domain consumers — `tools/fusha_nahw_particle_rules.py` (homograph harakāt · negation scope · state transitions), `tools/fusha_nahw_context_rules.py` (sense rivals · referent guards · jar-majrūr wording · enclitic attachment), `tools/fusha_nahw_gate_rules.py` (iʿrāb-safety tiers · topic map) — plus `tools/run_nahw_evals.py` (6 banks; 2 semantically fixture-only, packetized; 21 rows quarantined) and `tools/test_nahw_behavioural_gates.py` (5 mutation classes). There is deliberately **no** single generic naḥw resolver, and no rule's free-text `when` prose is executed: behaviour comes from a closed discriminator registry keyed by rule id plus the rule files' own structured fields. Consumption status is machine-enforced by `tools/validate_nahw_skill.py` (`RULES_CONSUMPTION`), where `consumed` requires a **distinct load-bearing on/off probe**: the production validator must fire on that file's own decision property AND stop firing when only that file's data is removed, while every other probe still fires (isolation). File existence, an import, a path reference, a self-reported status and a structural row check do **not** establish a consumer. Each helper's `FILE_CONSUMPTION` map must agree exactly with the authoritative inventory, so a helper can no longer call a fixture-gated row consumed. State-moving input must be a validated, immutable **observation artifact** (`typed_observation`): shape, authorized producer, canonical coordinate checked against the repository's own `tools/fusha_check.resolve_address` over the committed spine, binding to the exact token/surface/state/referent, and a content-integrity hash. An address-shaped string, a caller-minted producer, an impossible or newline-suffixed coordinate, a reused or surface-mismatched artifact and a mutated field all fail closed. Every candidate-producing consumer must also be told the caller's CURRENT exact address (`at=`): an internally valid same-surface artifact replayed at another occurrence is `occurrence_not_current`, and omitting the address is `caller_occurrence_absent` — never a candidate. Because no repository producer signs observations and the spine carries no per-word written surface, every registered producer is `fixture` trust and an artifact can yield at most `candidate` — packetized as `TP-NAHW-A2-OBSERVATION-PRODUCER`. A context sense is never authorized by `norm_strict` alone: trusted morphology (lemma, derived form, voice, POS) must confirm the lexeme, so Form II `يُقَدِّرُ` and passive `أُتِيَ` no longer inherit the Form I active tables. A registered reason key does not license an arbitrary conclusion: each key must satisfy a closed **licensing tuple** (conclusion, case/mood, governor type, fact type), with `fact_type` checked against the key's own `applicable_fact_types` in `qamus/skills/reason-key-registry.jsonl` and any key lacking repository tuple authority routed pending as `reason_tuple_unavailable`. So a fused-preposition genitive reason can no longer ride an accusative conclusion under a `verb_object` governor. A human/source review is bound to the exact claim it is offered for — same address, subject, conclusion and reason key, a canonical `hr:` identifier, an ISO-8601 timestamp and a reviewer distinct from the claimant — so a valid review of another address is not evidence here. LargeLexicon collision rows bind a typed `forbidden_reading_id`, so unrelated non-empty text can no longer stand in for a forbidden reading. Two-vote grading delegates the COMPLETE artifact contract (vote-id format, reviewer provenance, timestamp, occurrence, segmentation, lexical identity, root, form, conclusion shape, reason key) to `tools/validate_two_vote_artifacts.validate_vote()` before projecting for agreement, and layers engine/reviewer/lane/vote-id/worklist independence on top, requiring each `worklist_id` to be a valid non-empty canonical identifier BEFORE the two are compared (null, empty, whitespace and malformed ids are missing provenance, not different worklists); the worklist axis is an A2 addition, since the shared contract does not model worklists. A quarantine needs TYPED authority: a closed status vocabulary plus a packet **parsed as its structured artifact** carrying a machine-readable `QUARANTINE-BINDING` block (bank, status, exact row ids, uncovered property, permitted disposition). A fabricated file whose prose merely mentions the bank and row id has no `packet_id`, no binding block and no `NOT_DEPLOYED` record, so it authorises nothing. The only permitted disposition is `excuse_semantic_consumer`: gate strength and trigger/gate compatibility are checked for **every** row, quarantined or not, **before** any quarantine early return, so a quarantine can never hide a weakened gate. (Schema note: `qamus/schemas/task-packet.schema.json` is `additionalProperties: false` at every level and this lane may not widen it, so the typed binding lives inside the allowed `candidate_population.selection_rule` under a strict grammar.) Hover rows compare the consumer's exact decision, pending reason, reading and gloss against a committed binding — plus the bank's own `gloss_if_safe`, `safe`, `gate` and `key_fn`, so a wrong teaching gloss is caught even though no production consumer owns that axis (reported `bank_gloss_if_safe_axis: committed_binding_only_no_production_owner`) — and the bound/mutation counts are asserted so a neutered consumer cannot silently remove the adversarial axis; the production key-function selection lives in the rendering layer and is reported `key_selection: no_production_authority` rather than asserted. `run_nahw_evals.py --json` emits exactly one JSON document with a matching exit code. The grader validates reason keys against `qamus/skills/reason-key-registry.jsonl`, never certifies (agreement routes to `candidate_agreed_pending_certification`), requires a typed `expected_conclusion` compared exactly, treats `human_source_review_required` as a distinct gate needing its own validated independent review record (`two_vote_done` cannot substitute), and implements **no** candidate `nahw@2.1`–`@2.4` behaviour in the released path.

Terminology note: **VN** in this repo means a page-range tranche over the authoritative `source_key` ordering of the 2,092 Qamus entries (947 verbs + 1,045 nouns + 100 particles); e.g. VN-00 = `v001-v047 + n0001-n0045` (`docs/VN-OPERATIONS.md:18-27`, `docs/GLOSSARY.md:70`). It is not "verbal noun".

## 1. What the nahw subsystem is

Nahw is the syntax/context disambiguation layer after norm → ṣarf (`nahw/README.md:25-29,56-62`): particle function, governor/iʿrāb, referent, negation scope, homograph resolution by context. Skill text `nahw/SKILL.md` (489 lines): grammar-safety ANDON — "a correct answer with wrong iʿrāb reasoning is unsafe"; two independent checks must agree on **conclusion AND reason** (`nahw/SKILL.md:12-19`); 25-link procedure index (`:21-46`); output contract JSON with `function_class`, `attachment`, `composition`, `parse_key`, `display.segments`, `decision`, `allowed_for_hover` (`:87-105`); governor/dependency-lattice section (`:275-314`); six nahw principles (`:318-326`).

Released tier is naḥw@2. Four CANDIDATE increments are appended, each stating "NOT released; does not amend @2": @2.1 8 rules (`nahw/SKILL.md:374-406`), @2.2 3 rules (`:408-427`), @2.3 2 rules (`:429-448`), @2.4 4 rules (`:450-488`). Machine rows live in `qamus/skills/rule-registry-increment-2N.jsonl` with discriminators `tools/skill_fixtures/skill_rules_increment2N.py`. All 17 nahw increment rows are `status: candidate` (GAP-N1). The registry row schema — where `status`, `projector.guards`, `defeaters`, `abstention_condition` actually live — is uniform with the sarf registry (see `docs/subsystems/sarf-executable-map.md` §2); `mood_basis` is a two-vote-artifact field, not a registry field.

## 2. Rules files (`nahw/rules/`, 12 files)

None of the 12 carries `basis`/`defeaters`/`status` fields; guard fields appear in `particle-context-rules.json` and `referent-guard-rules.json` only.

| File | Entries | Example |
|---|---|---|
| `context-sense-rules.json` | 2 rules + negation_sense_rule + polyseme quarantines | `qadara_contronym` (`:6`), `la_nafiya_lil_jins` (`:61`) |
| `funcword-homograph-prepass-rules.json` | 3 rules (`qamus.funcword_homograph_prepass_rules.v1`, `:57`) | `man_fatha_not_min` (`:21`) |
| `grammar-problems-gates.json` | 12 topic gates + 2 difficulty rules | `irab: {gate: two_vote_required, hover: never_auto}` (`:5`) |
| `grammar-problems-issue-clusters.json` | 5 clusters (`fusha/grammar-problems-issue-clusters@1`) | `issue_1_wrong_reasoning` (`:14`) |
| `irab-safety-gates.json` | 6 rules | `irab_assignment` requires "reasoning present" (`:5`) |
| `negation-rules.json` | 5 rules | `lam_jussive` (`:7`) |
| `particle-context-rules.json` | 8 rules with `distinguish_by`/`guard`/`pending_fallback` (7 haraka-based + 1 occurrence-specific contextual-frame based) | `man_vs_min` (`:7`), `maa_relative_vs_negation` (`:205`) |
| `preposition-pronoun-rules.json` | 4 rules, `renderings_by_referent` | `bihi` (`:7`), `ilayna` (`:37`) |
| `pronoun-attachment-rules.json` | 3 host rules + 2 forbidden + 2 two-vote triggers | `verb_host` (`:5`) |
| `referent-guard-rules.json` | 3 rules + 3 general guards | `halim_referent` (`:7`) |
| `state-transition-rules.json` | 10 transitions + 5 forbidden | `an_light_to_masdariyyah` (`:15`), `min_not_man` (`:124`) |
| `two-vote-required-rules.json` | 12 two-vote + 5 human-review + 5 never-auto triggers | protocol `must_agree_on: [conclusion, reasoning]` (`:36`) |

**Real SSOT enforcement:** `two-vote-required-rules.json` is a verified copy of `nahw/evals/grammar-decision-gates.json`; four-home parity is mutation-proven in `tools/test_gate_ssot.py:20-60` and asserted at `tools/check_regressions.py:128-144` ("grammar trigger lists match SSOT across all 4 homes").

Executable-consumption honesty **(post-A2 defect round 2, measured by `python tools/validate_nahw_skill.py`)**: **5 of 12** files are `consumed`, each proven by its OWN isolated on/off probe against `tools/validate_linguistic_decisions.py` — `context-sense`, `particle-context`, `preposition-pronoun`, `referent-guard`, `state-transition`. The other **7 are `fixture_gated`**: `funcword-homograph-prepass`, `grammar-problems-gates`, `grammar-problems-issue-clusters`, `irab-safety-gates`, `negation-rules`, `pronoun-attachment-rules`, `two-vote-required-rules`. Round 1 reported 7/12; exact probes support 5. The pre-A2 state was 5 files with zero consumer (GAP-N2) and 2 existence-checked only (GAP-N3).

| File | Status | Consumer | What is actually executed |
|---|---|---|---|
| `context-sense-rules.json` | consumed | `fusha_nahw_context_rules.py` | `polyseme_quarantines` as a blocked-sibling-sense guard; `rules[]` returned as **preserved rivals** (the `when` prose is documentary and never selects a sense) |
| `funcword-homograph-prepass-rules.json` | fixture_gated | `funcword_homograph_prepass.py:22` + 13 tests | a real consumer exists, but A2 added no on/off probe, so A2 does not count it as proven production-consumed |
| `grammar-problems-gates.json` | fixture_gated | `fusha_nahw_gate_rules.py` | `by_topic` gate/hover map, reconciled **upward** against the gates SSOT; `by_difficulty` prose stays documentary |
| `grammar-problems-issue-clusters.json` | fixture_gated | `check_regressions.py:1292` | existence + fixture only |
| `irab-safety-gates.json` | fixture_gated | `fusha_nahw_gate_rules.py` | per-rule tier + triggers; `validate_rule_files()` rejects an `auto_safe` iʿrāb tier, an off-SSOT trigger, or a tier weaker than its own triggers require |
| `negation-rules.json` | fixture_gated | `fusha_nahw_particle_rules.py` | `governs`/`effect`/`gloss` per surface, behind the file's own `standing_order` (homograph first) |
| `particle-context-rules.json` | consumed | `fusha_nahw_particle_rules.py` | 7 rules have registered discriminators over `normalize_ar` observations (branch text read from `decision_if_A/B`, absent diacritic yields the file's `pending_fallback`); the 8th, `maa_relative_vs_negation`, carries no content-letter diacritic and is instead read by `maa_context_frame()` reading `frame_table` (occurrence-specific CONTEXTUAL FRAME, never the conclusion label) plus `recognized_unmapped_frames` for a frame the mechanism can report but this table deliberately does not map. I7: the file's `consumed` status is earned by the 7 haraka rules through the UNRELATED `haraka_reading` probe; `maa_context_frame()` has its own DISTINCT, isolated on/off probe in `validate_nahw_skill.py`, but that probe is callable-level and data-driven, not a production record-validation path — the callable's only callers are `tools/run_nahw_evals.py`, `validate_nahw_skill.py` itself and `tools/test_nahw_behavioural_gates.py`, exactly the standard under which `negation-rules.json` is held `fixture_gated` rather than `consumed` elsewhere in this table |
| `preposition-pronoun-rules.json` | consumed | `fusha_nahw_context_rules.py` | `renderings_by_referent` under a closed referent vocabulary; the إِلَيْنَا ≠ ل-ي-ن `hard_guard` is executable |
| `pronoun-attachment-rules.json` | fixture_gated | `fusha_nahw_context_rules.py` | host-POS `attachment` map + `forbidden` table + `two_vote_required` triggers, read by `tools/test_suffix_pronoun.py` (a TEST, so fixture-gated, not production-consumed) |
| `referent-guard-rules.json` | consumed | `fusha_nahw_context_rules.py` | referent decision tables under a closed class vocabulary; `proper_noun_not_verb` examples as a POS guard |
| `state-transition-rules.json` | consumed | `fusha_nahw_particle_rules.py` | the 5 `forbidden_transitions`, bound to a resolved (rule, branch) pair and **production-enforced** on real records by `validate_linguistic_decisions.py` with its own isolated on/off probe. The 10 `transitions[]` rows are **documentary**: they fire-or-hold inside the consumer and its tests (a transition now also requires its from-state — the written surface resolved to the bound rule/branch and shadda weight, or the declared from-state token — and yields `pending` otherwise), but no production path calls them |
| `two-vote-required-rules.json` | fixture_gated | `test_gate_ssot.py` | four-home SSOT parity, mutation-proven — but a TEST, not a production decision, and A2 added no on/off probe |

The five `consumed` files are exactly those enforced on real decision records: `tools/validate_linguistic_decisions.py` now rejects a forbidden role collision (the whole مِن/مَن، لَمْ/لِمَ، لَمَّا/لِمَا، أَنَّ/إِنَّ، إِنْ/أَنْ table, not a hand-copied pair), a polyseme-quarantine leak, a proper-noun verb gloss, and the إِلَيْنَا root guard — proven by three red-first self-test records (bad 10–12) and by five isolated on/off probes.

## 3. Governor / relation model (dependency candidate lattice)

Schema `qamus/schemas/dependency-candidate-lattice.schema.json` — "Fusha dependency / governor (iʿrāb) candidate lattice" (`:4`), `schema: fusha/dependency-candidate-lattice@1`:

- `input_mode`: `source_addressed | corpus_backed | arbitrary_typing` (`:46`).
- Edge fields (`:112`): `edge_id, dependent, headless, governor_type, rel_label, rel_label_ar, …`; `governor_type` `verb|noun|particle|none` (`:121`); `governor_family` incl. `jussive, inna_family, la_jins, istithna, hal, vocative, kana_family, null` (`:135`); `justification_rule` closed enum (`preposition_governs_genitive, idafa_governs_genitive, verb_governs_subject_nominative, …`, `:155`); `evidence_class` `source_addressed|heuristic|unknown` (`:161`); `decision_status` (`:169`); `gate` ladder (`:170`); routing `lane` incl. `scholar_irab_review` (`:176`).
- Builder: `tools/fusha_governor.py` — `build_dependency_lattice()` (`:173`); `governor_not_justified` routes to `scholar_irab_review` + `nahw/procedures/irab-case-mood.md` (`:94`); hard assert "governor edge may never be auto_safe" (`:326`); self-test (`:355-396`).
- Validator: `tools/validate_dependency_lattice.py` — `_bad_lattices()`'s own self-test covers 24 hostile lattices across 18 closed condition labels (`1`-`13`, `15`-`18`, `schema`): case/mood without justification, ambiguous-marked-resolved, any `auto_safe` edge, right-answer-wrong-reason under-gated, heuristic-resolved, `summary.live_writes != 0`, family-less abrogators, invalid attribution, frame_kind/address agreement, contradictory mabnī/maḥall state, unlicensed hidden elements, and preposition reasoning on an annexation head. Occurrence identity (FAIL 14) is a SEPARATE `--verify-occurrence-identity` check (`verify_occurrence_identity()`), not one of the 18 self-test labels — M4: it is verified directly by `tools.test_nahw_governor_families`'s `G3ToG6OccurrenceIdentity` class and (since the I6 repair) by `check_regressions.py`'s per-bank lattice pass below, not by `_bad_lattices()`.
- Shared constant: `IRAB_SENSITIVE_ISSUE_CLASSES` incl. `governor_not_justified` (`tools/fusha_check.py:44-48`), imported by rich-hover and text-check validators.
- Harness gating: `tools/check_regressions.py` runs the schema/builder/validator self-tests, the committed dependency-lattice fixture, all three Train B family banks through `fusha_governor.py --run-bank` AND (I6 repair) through `validate_dependency_lattice.py --verify-occurrence-identity` over the banks' OWN built lattices (address-bearing/constructed rows; the four deliberate refusal-control rows are excluded from this pass and verified separately), and `tools.test_nahw_governor_families` for semantic and occurrence-identity assertions.

### Train B curriculum operationalization — governor/relation families (2026-08-03)

Three candidate-only banks now exercise the ordinary governor lattice rather than a curriculum-local consumer: `nawasikh-government-eval.jsonl` (18 rows, wave-1: +F14, the 7:56-shaped iḍāfa/khabar-boundary pin), `coordination-case-following-eval.jsonl` (11), and `hidden-structure-mahall-eval.jsonl` (13), for 42 rows total. They cover occurrence-addressed and constructed cases across nawāsikh regimes, following-token-sensitive `لَا`, coordination/case following, token-internal conjunction abstention, annexation heads, hidden elements, taqdīr, and maḥall. The 14 address-bearing positive frames are resolved against canonical occurrence identity by the test suite; four negative controls must be refused (each asserted against its OWN declared refusal mechanism, not merely "some error" — three are ayah-absent-from-spine coverage accidents, one is the genuinely semantic word-index-overrun control N3 models).

The consumer preserves candidate posture: no abrogator edge is `auto_safe`; syncretic or missing marking, ambiguous bracketing, sense-dependent licensing, and cross-token-layer uncertainty abstain; attributed rivals remain alternatives; a correct case with the wrong governor/reason still fails. An explicitly adjective-typed dependent is never emitted as a muḍāf ilayh merely because it follows a noun with matching case; it remains an unresolved ṣifa/badal posture. Nothing in this batch certifies an occurrence or authorizes public projection.

**Two-vote v1.1 and `mood_basis`:** `qamus/schemas/two-vote-artifact.schema.json` — v1/v1.1 (`:4,:20`, conditional v1.1 block `:58`); `case_or_mood.mood_basis ∈ governed|tajarrud|default` (`:218-221`); `governor` required only when `mood_basis=governed` (`:206`). Enforced in `tools/validate_two_vote_artifacts.py`: `mood_basis` must be null when value=none (`:304`), governed-without-governor is an error (`:310-311`), `tajarrud/default only licenses raf3` (`:316`); legacy v1 nominal rule retained at `:389-392`.

## 4. "Bare label is not pedagogically complete"

The literal phrase does not appear in the repo; the nearest skill-text anchor is `nahw/SKILL.md:260` ("…must be visible where the public hover claims pedagogical completeness"). The doctrine is executable across four validators:

1. `tools/validate_segment_completeness.py` — the direct implementation: seven fail-closed completeness gates that "reject internally-consistent but pedagogically incomplete rows" (`:16-18`). Gate A `governor_to_segment` (asserted government must show its visible segment, `:20-21`); C `feature_to_display` (claimed prefix/suffix/article/pronoun must appear in `segments[]`, `:23-25`); E `exact_occurrence` (source-addressed records may not fall back to "as context requires", `:27-29`); F `explanation_truth` (`:30-31`).
2. `tools/check_rich_hover_norm.py` — `N-PED-01`: every segment carries a non-empty `gloss_contribution` (`:225-227`); `N-PED-02`: morphline may not hedge committed features (`:229-232`); `N-LANG-01` blocks internal meta-language and leaked label notation (`:65-74,:198-199`).
3. `tools/validate_nahw_skill.py:88-101` — irab-polysemy rows IP-026…IP-034 must carry `composition`, `attachment`, and `reject_host_only_gloss === True` (a host-only gloss is machine-rejected).
4. `tools/validate_vn00_aggressive_false_closure.py` — failure classes naming the defect: `tanwin_or_case_visible_but_unexplained` (`:84-101`), `common_particle_role_underexplained`, `preposition_pronoun_undersegmented`, `article_or_proclitic_not_explained` (`:50-55`).

Doc-level (non-executable): `docs/qamus/particle-projection-contract.md:84-102` — "Rootlessness is taught, never blank"; no Arabic iʿrāb prose in the teaching plane. There is no single named `bare_label` gate; the doctrine is distributed (GAP-N6).

## 5. Particle ↔ VN bidirectional constraint

Artifacts: `vn-ledger.jsonl` (7,740 rows = D3 displayed selected-word records; source-key distribution v 4,558 / n 2,916 / p 266), `vn-readiness-matrix.json`, `vn-graph-metrics.json`, `VNMAP-REPORT.md` (input spaces 100 p / 947 v / 1,045 n `:9-13`; denominators D1=2,092, D2=7,700, D3=7,740, D4 34,323 unique / 56,117 total `:19-27`).

Enforced bidirectionality:

- Ledger level: `tools/validate_vn_ledger.py:124-125` — every row with an `occurrence_id` must have `reverse_entry_relationship_present`; owner-enum `missing_edges` ordering (`:110-122`); denominator sum-check (`:139-140`); red-first mutations incl. reciprocity break (`:241-265`). Gated at `tools/check_regressions.py:5709-5737` (v2 at `:5793-5818`).
- **The worked particle↔VN fixture is the p007 لِـ pilot** (`qamus/examples/p007-li-pilot/`, 23 files). Particle→VN direction: `vn-unlock.json` — 12 certified occurrences → 78 appearances across `VNPROP-00…19` / `P-00…03`. VN→particle direction: `tools/validate_p007_pilot.py` — reverse edge kind `particle_entry_reverse_occurrence_edge` (`:81`); reverse edges must cover exactly the 12 certified occurrences (`:287-288`); reverse-trace closure gate 5 (`:307-347`); page-class totals must be `n:22 / v:54 / p:2` (`:388-393`); red-first `break_reverse_index` (`:514-534`).
- Rerun hooks: the pilot IS executable and deterministic offline — `tools/build_p007_li_pilot.py:35-37` ("Stdlib only. Deterministic: same inputs → byte-identical outputs"), gated at `tools/check_regressions.py:5959-5990`. The full-corpus VN ledger rebuild is **manual/external**: `VNMAP-REPORT.md:148` requires five explicit input paths not present in the repo; CI validates only the fixture subset (GAP-N8).
- Ceiling honesty: `vn-unlock.json#pattern_ceiling_li_kasra_clitic` — 580 matrix rows / 1,266 appearances is a ceiling only; each row still needs per-occurrence host-noun evidence (rejections cited at 48:29:42 and 2:187:9). Reporting 1,266 as coverage would be false closure (GAP-N9).

Particle projection contract: `docs/qamus/particle-projection-contract.md` (status **proposed** 2026-07-29, `:3`) — rich-at-rest + 13-item rich-hover teaching order, projection-hash cross-page parity (`:104-140`), per-occurrence artifact sharing (`:144-152`), candidate-vs-certified planes (`:154-175`). Executable twin `tools/validate_particle_projection_parity.py` (722 lines; parity hash, per-occurrence, rootless pedagogy, no-iʿrāb-prose, lattice/edge/vocabulary checks at `:131-329`; "Where this prose and the validator disagree, the validator is the bug and this doc is the spec", `:18-21`), gated at `tools/check_regressions.py:5859-5893` against the committed fixture sample `qamus/examples/particle_projection_parity.sample.json` only. `qamus/schemas/particle-edge-ontology.schema.json:5` — a certified iʿrāb-bearing edge structurally requires a two-vote artifact reference; the homograph candidate lattice allows at most one certified winner (`:402`, `maxContains: 1`). The template deck `docs/qamus/particle-rich-hover-templates.md` is proposal-status with no validator consumer (GAP-N7).

## 6. Per-category status

| Category | Rules data | Executable gate | Fixtures/evals | GAP |
|---|---|---|---|---|
| Homograph function words | `funcword-homograph-prepass-rules.json` (3), `particle-context-rules.json` (8: 7 haraka + 1 occurrence-frame), `state-transition-rules.json` forbidden set | `tools/funcword_homograph_prepass.py` + 13 tests; **`tools/fusha_nahw_particle_rules.py`** (7 discriminators + 5 forbidden bindings + `maa_context_frame()` occurrence-frame discriminator), enforced on records by `validate_linguistic_decisions.py` | prepass tests; `particle-function-eval.jsonl` (119); **`nahw-state-machine-eval.json` (24) replayed with evidence-ablation**; **`ma-function-occurrence-eval.jsonl` (6) replayed through `maa_context_frame()` plus 24 cross-occurrence replay probes** | GAP-N2 **PARTIAL**: particle-context and state-transition are production-consumed with distinct probes |
| Governor / iʿrāb | lattice schema + `irab-safety-gates.json` (6) | `fusha_governor.py`, `validate_dependency_lattice.py` (24 hostile lattices / 18 condition labels + FAIL 14 occurrence identity verified separately), gated `check_regressions.py`'s p2 slice; **`tools/fusha_nahw_gate_rules.py`** consumes all 6 iʿrāb-safety rules | `governor-dependency-lattice.jsonl` (7 rows, replayed via `tools/replay_sarfnahw_evals.py:48-49`); Train B's 3 governor banks (42 rows, see §3 Train B note) now also clear the validator with occurrence identity, not just `--run-bank` | irab-safety-gates.json is read only by the A2 gate helper and its tests, so it stays **fixture_gated**; still only 7 base lattice-schema rows |
| Right-answer-wrong-reason | `grammar-problems-issue-clusters.json` (5) | `tools/grade_grammar_reasoning.py` — the boolean AND-gate **plus `grade_structured()`/`grade_two_vote()`, which DERIVE the reasoning verdict from a registered reason key + a governor whose type licenses the asserted case** (`fusha_governor.GOVERNOR_TYPE_LICENSED_CASES`), ignoring any caller-supplied `reasoning_ok`, and (round 8) requiring **two explicit, contract-valid, independent vote records agreeing on the complete conclusion-and-reason tuple** before a two-vote gate is satisfied — `two_vote_done=True` is no longer proof of anything, `source_address` is resolved through `tools/fusha_check.resolve_address` rather than tested for non-emptiness, and a missing `fact_type` is an error rather than being defaulted to the expected value; `tools/run_grammar_evals.py` | `grammar-problems-derived-eval.jsonl` (88); `irab-right-answer-wrong-reason.jsonl` (6, replayed); **`grammar-wrong-reasoning-cases.jsonl` (8) now run by `tools/run_nahw_evals.py --bank wrong-reasoning`** | GAP-N4 **PARTIAL**: the bank runs, but the semantic comparison is fixture-only |
| Negation / scope | `negation-rules.json` (5) | **`tools/fusha_nahw_particle_rules.py` `negation_effect()`** (standing-order enforced: homograph first, then the negation effect) | behavioural gates in `tools/test_nahw_behavioural_gates.py` (لَمْ resolves; bare لم، مَا، لَا hold pending) | GAP-N2 **PARTIAL**: fixture-gated, no production caller |
| Preposition + pronoun / referent | `preposition-pronoun-rules.json` (4), `referent-guard-rules.json` (3) | **`tools/fusha_nahw_context_rules.py`** (`preposition_pronoun_render`, `referent_gloss`, `root_guard_violation`, `proper_noun_verb_violations`), enforced on records by `validate_linguistic_decisions.py` | `suffix-pronoun-eval.jsonl` (71); `tools/test_suffix_pronoun.py` gated `:472-473` | GAP-N2 **PARTIAL** |
| Pronoun attachment | `pronoun-attachment-rules.json` | `tools/fusha_nahw_context_rules.py` `attachment_role()` / `is_forbidden_attachment()`, read by `tools/test_suffix_pronoun.py` (was existence-check only at `check_regressions.py:234`) | irab-polysemy IP rows with `reject_host_only_gloss`; 71 suffix rows | GAP-N3 **PARTIAL** — fixture-gated, no production caller |
| Polysemy / context sense | `context-sense-rules.json` (2+) | `validate_nahw_skill.py` (17 required regression surfaces) + **`tools/fusha_nahw_context_rules.py` (rivals preserved, quarantines enforced)** | `irab-polysemy-eval.jsonl` (130) | GAP-N2 **PARTIAL**; the `when` prose stays deliberately documentary |
| Hover safety / surface keys | `particle-context-rules.json` | **`tools/run_nahw_evals.py --bank hover-context`** — recomputes each key with `normalize_ar`, requires a real sibling collision, and re-derives `safe` from the consumer, then strips the harakāt adversarially | `hover-context-eval.json` (20; 2 quarantined → `TP-NAHW-A2-HOVER-KEY-SEAT`) | GAP-N4 **PARTIAL**: the bank runs, but the semantic comparison is fixture-only |
| Largelexicon collision safety | `nahw/SKILL.md` §9 inventory | **`tools/fusha_context_parser.py` `collision_status()`** — short function-like surfaces route to `pending_context` naming the forbidden lexical row | `largelexicon-function-collision-safety.jsonl` (3) | GAP-N4 **PARTIAL**: the bank runs, but the semantic comparison is fixture-only |
| Public-boundary scanning | `provenance/source-boundaries.md` §3 | **`tools/run_nahw_evals.py --bank public-boundary`** replays each gloss through `tools/leak_sot.LEAK_RE` | `public-boundary-scanner-eval.jsonl` (10: 6 leak / 4 clean word-boundary negatives) | GAP-N4 **PARTIAL**: the bank runs, but the semantic comparison is fixture-only |
| Two-vote / gates SSOT | `two-vote-required-rules.json` | `test_gate_ssot.py` 4-home parity; `validate_two_vote_artifacts.py` v1/v1.1 | mutation-proven | — |
| Clause relations / exceptions (istithnāʾ, ḥāl, etc.) | asserted `nahw/SKILL.md:226-230`; `governor_family` enum has `istithna, hal, la_jins, vocative` | schema enum only; no dedicated construction handler | F7 (`hidden-structure-mahall-eval.jsonl`, لَآ إِلَٰهَ **إِلَّا** هُوَ) drives the `istithna` trigger through the generic `abstained_unmodeled_construction` fallback (M4: not "no fixture" — recognition only, no dedicated istithnāʾ analysis) | GAP-N10 |

Inventory: 28 procedures, 9 references, 8 drills (1,381 lines), 5 curriculum files, 22 eval files (13 jsonl / 4 json / 5 md), 2 example jsonl (10 + 24 rows). `tools/validate_nahw_skill.py` requires 20 procedures, 6 references, 3 evals, 2 drills, ≥25 particle-function cases, 16 required particles, 17 iʿrāb-polysemy surfaces, and MCP-freedom (`:23-112`); gated `check_regressions.py:3336-3337`. Since A2 it is **no longer structural-only**: it also runs behavioural probes — five DISTINCT isolated on/off probes that decide which rules files are production-consumed, live positive/negative exercise of the particle discriminators, and the structured right-answer/wrong-reason property through `grade_structured()`. What remains open under GAP-N5 is the typed state/function and wrong-reason banks, not the validator's own nature.

## 7. Pipeline participation

1. **Candidate generation** — `tools/corpus_to_hover_decisions.py:5-13` (`particle_or_construction` verdict routes to nahw procedures); `tools/export_token_hover_decisions.py:8,70` (same-surface polysemy left pending "needs iʿrāb"); `tools/fusha_governor.py`; `tools/build_rich_hover_morphosyntax_candidates.py:198-202`; `tools/funcword_homograph_prepass.py`.
2. **Gating/certification** — `nahw/evals/grammar-decision-gates.json` SSOT consumed by `tools/build_two_vote_packets.py:52` (packets carry `nahw_evidence`, `:75,:1100`), `build_funcword_two_vote_packets.py`, `build_rebind_two_vote_packets.py`; artifacts validated by `validate_two_vote_artifacts.py`; conflicts surfaced (never side-picked) by `tools/fusha_conflicts.py`.
3. **Projection** — `build_canonical_hover_payload_table.py` + validator; `check_rich_hover_norm.py`; `validate_segment_completeness.py`; `validate_particle_projection_parity.py`; false-closure ANDON `validate_vn00_aggressive_false_closure.py` + `validate_meta_transclusion_projection.py` (gated `check_regressions.py:3344-3352`).
4. **Drills/tutoring** — `tools/fusha_learner_feedback.py:5-9` (KC violation record; bottom-out withheld unless `gate==auto_safe ∧ decision_status==resolved ∧ ¬right_answer_wrong_reason`; hints never downgrade a gate); CEFR depth gate `tools/fusha_cefr_gate.py` (iʿrāb terminology C1+ only, `nahw/SKILL.md:295-296`); back-propagation validators `validate_sarf_nahw_skill_backprop.py`, `validate_sarf_nahw_curriculum_drills_readmes.py` (gated `:3330-3333`).

## 8. GAPs (honest, with work-packet stubs)

- **GAP-N1 — all 17 nahw increment rules candidate-only**, none released (increment files 21/22/23/24). WP-NAHW-INCREMENT-RELEASE-REVIEW.
- **GAP-N2 — PARTIAL (A2, 2026-07-30).** 5 of 12 rules files are load-bearing in a production path, proven by five DISTINCT isolated on/off probes in `validate_nahw_skill.py`; 7 remain fixture-gated because only tests/validators read them (`funcword-homograph-prepass`, `grammar-problems-gates`, `grammar-problems-issue-clusters`, `irab-safety-gates`, `negation-rules`, `pronoun-attachment-rules`, `two-vote-required-rules`). The **five production-consumed files are exactly**: `context-sense-rules.json`, `preposition-pronoun-rules.json` and `referent-guard-rules.json` → `tools/fusha_nahw_context_rules.py`; `particle-context-rules.json` and `state-transition-rules.json` (its `forbidden_transitions` table) → `tools/fusha_nahw_particle_rules.py`. `negation-rules.json` and `irab-safety-gates.json` DO have bounded consumers (`tools/fusha_nahw_particle_rules.py` and `tools/fusha_nahw_gate_rules.py` respectively) but are **fixture-gated, not production-consumed** — no on/off probe shows a production decision changing with their data — so they are not among the five and must not be counted as such. Consumption status is machine-enforced by `validate_nahw_skill.py#RULES_CONSUMPTION`, so a new unconsumed rules file fails the gate. Residual: each rule file's free-text `when`/`why_forbidden` prose stays **documentary by design** — it is exposed as preserved rivals, never executed.
- **GAP-N3 — PARTIAL (A2).** The `forbidden_transitions` table is load-bearing in `validate_linguistic_decisions.py` and has its own isolated probe; the 10 `transitions[]` rows are `documentary` (no production caller) and `pronoun-attachment-rules.json` is fixture-gated only. `state-transition-rules.json` fires/holds all 10 transitions and binds all 5 `forbidden_transitions` to a resolved (rule, branch) pair, enforced on real records by `validate_linguistic_decisions.py`; `pronoun-attachment-rules.json` is load-bearing inside `tools/test_suffix_pronoun.py`. The full collision table (مِن/مَن، لَمْ/لِمَ، لَمَّا/لِمَا، أَنَّ/إِنَّ، إِنْ/أَنْ) is machine-enforced from the file rather than hand-copied.
- **GAP-N4 — PARTIAL (A2, extended wave-1).** All 7 execution groups run, over 8 physical artifacts, but **5 of the 8 artifacts are `fixture_only`**: `grammar-wrong-reasoning-cases.jsonl` is prose-only (→ `TP-NAHW-A2-TYPED-WR-BANK`); `nahw-state-machine-eval.json` compares particle IDENTITY plus evidence ablation, not the typed state (→ `TP-NAHW-A2-TYPED-STATE-CONSUMER`); `hover-context-eval.json` decides only 8 of its 20 rows; and `particle-function-eval.jsonl` + `irab-polysemy-eval.jsonl` are cross-checked structurally. `public-boundary-scanner-eval.jsonl` (10 rows), `largelexicon-function-collision-safety.jsonl` (3 rows) and `ma-function-occurrence-eval.jsonl` (6 rows) are `implemented_and_consumed` — **19 behaviourally credited rows of 320**. Structural row checks and the **21** quarantined rows are excluded from any closure claim. Round 2 additionally quarantined five semantically malformed state rows (`lamma_not_yet`, `la_nafiya_lil_jins`, `ma_relative_vs_negation`, `anna_how_adverbial`, `nima_praise_verb`) and ten hover rows that no consumer decides, and every non-quarantined row now carries an EXACT committed binding — a missing binding is a failure, never skipped coverage. All five runnerless banks — plus a sixth, `function-polysemy`, which upgrades the previously existence-only `particle-function-eval.jsonl` (119) and `irab-polysemy-eval.jsonl` (130), and a seventh, `ma-function-occurrence` (occurrence-specific contextual مَا relative-vs-negation, wave-1) — run under `tools/run_nahw_evals.py` (**7 banks, 320 cases**, behavioural not existence): state-machine with evidence-ablation mutation, hover-context with key recomputation + harakāt stripping, wrong-reasoning through the structured grader, llx-collision through the parser routing table, public-boundary through the leak SoT, function-polysemy cross-checking the particle consumer against the bank's homograph rows plus the no-bare-slot composition property, and ma-function-occurrence replaying each row's typed contextual-frame observation through `maa_context_frame()` plus a cross-occurrence replay probe (24 probes) proving the evidence cannot be reused at another exact address. Two contract assumptions were tested and **rejected** rather than encoded: `context_function` is not a member of the `functions` inventory (they are separate vocabularies, 84 rows), and a host-only gloss is not detectable by word count (a vocative's whole contribution is one word). Six state/hover rows are **quarantined with explicit contract defects and packets** — 4 → `TP-NAHW-A2-STATE-VOCAB` (correct_state == wrong_state), 2 → `TP-NAHW-A2-HOVER-KEY-SEAT` (declared sibling surfaces collide under no repository key function); `ma-function-occurrence-eval.jsonl` carries no quarantine — every row is decided. Runner integration into `tools/check_regressions.py` is **done** (serialized integration round): the consumer self-tests, the behavioural/mutation suite and the eval runner all execute there, the `--json` result must be exactly one parseable document from a genuinely successful run (`ok is True`, return code 0, `errors == []`), and the artifact set, row denominators, dispositions, consumer ownership, observed call counts, the 19 credited rows and the 21 quarantines are asserted exactly.
- **GAP-N5 — PARTIAL (A2).** `validate_nahw_skill.py` keeps its structural checks and adds a behavioural block: truthful status for every rules file and every rule id, live positive/negative exercise of the particle discriminators, enforcement of the forbidden-collision table, rival preservation, no `auto_safe` iʿrāb tier, and the structured right-answer/wrong-reason property. `tools/test_nahw_behavioural_gates.py` adds five mutation classes (wrong conclusion, wrong/absent reason, absent governor, lost rival, unsafe auto-resolution) including in-memory rule mutations that prove the consumers are data-driven rather than hard-coded.

**Round-10 semantic hardening (occurrence, canonical two-vote, homograph).** An OCCURRENCE is now one
written word at one exact coordinate: `quran:S:A:W` with `1 <= W <= the ayah's canonical token count`. An
ayah-only address and word index 0 both used to resolve `in_scope_source_addressed`, so evidence about a
whole verse could be bound to the word under decision. Written-surface binding is NFC-preserving exact
equality on every structured-grading, human-review, two-vote and typed-observation path — `norm_strict` had
been used, which folds the harakāt, so `حَلِيمٌ` and `حَلِيمٍ` compared equal. Homograph selection fails
closed unless EVERY discriminating mark the rule file itself names agrees, so `مُن`, `لَمَ`, `لِمْ`,
unvowelled `أن`, `إِنِّي`, `أَنِّى`, `أَنَّي`, `كَل`, `كُلَا` and `نَعِم` are all pending rather than
resolved; a public surface gloss is refused unless the written surface determines one member, so bare
`من → whoever`, `إِنِّى → that I`, `إِنَّ → if` and `إِنْ → indeed` cannot pass as occurrence truth. Rivals
are a transition's own local collision set with registered gate values, unresolved until evidence defeats
them, and a transition with no authorized lattice preserves an exact blocker. The two-vote path consumes the
CANONICAL artifact envelope (`qamus/schemas/two-vote-artifact.schema.json` plus
`tools/validate_two_vote_artifacts.validate_row`); `worklist_id` and `fact_type` are external worker-record
fields and are no longer written into vote records, which the schema forbids. A declared `two_vote_done`
boolean no longer satisfies a two-vote gate from ANY entry point.

**Round-10 gap status:**

- **GAP-N12 — CLOSED by the bounded certifier and p007 migration repair.** A two-vote certification needs all three:
  the artifact occurrence among its source addresses, the artifact's exact written surface in its
  `surface_spans`, and a complete type-specific `fact_value` projection equal to the votes' agreed conclusion
  **and reason key**. `governor_relation`, `case_mood_governor`, `contextual_function`, and `irab_rendering`
  have separate closed projections. Same-location value substitution, fact-type relabelling, reason-key drift,
  and v1/v1.1 surface drift are red-first regressions in `tools/test_certify_typed_fact.py`. `validate_trail()`
  re-opens the evidence behind every currently certified two-vote fact. The p007 trail preserves its 147
  historical events, append-only revokes the 36 location-only claims, certifies 36 exact v1.1 successors, and
  regenerates the current fact table and every downstream claim/hash consumer through
  `tools/migrate_p007_claim_binding.py`; `certification/claim-binding-migration.json` binds the 156-event
  migration (including 12 reverse-dependency rebinds) and terminal trail hash. This closes exact
  two-vote claim binding only: the p007 occurrence-to-sense edge remains candidate because the
  contextual-function fact does not certify dictionary sense identity, and uncompared
  `governor.relation` prose is withheld from the candidate projection.
- **GAP-N13 — the canonical contract still requires different `engine` values for independence.** Two
  genuinely independent reviewers can share a CLI surface, so A2's own independence rule no longer treats a
  shared engine as dependence; `tools/validate_two_vote_artifacts.py` still does, and it is not editable
  here, so such a pair is refused by the canonical contract rather than by A2.
- **GAP-N14 — what two-vote independence does NOT prove.** The canonical artifact carries no CLI version, no
  proof of which model actually produced a vote, no frozen artifact hash, no worklist provenance and no
  reviewer-isolation attestation. Those remain EXTERNAL worker-record requirements for the owner two-vote
  lane; A2 must not be read as establishing them.
- **GAP-N15 — one state-machine row has an undeterminable written identity.** `anni_vs_anna_how` asserts
  "final yāʾ ـِي, not alif-maqṣūra ـَى" while its own surface `أَنِّى` ends in alif-maqṣūra, and
  `irab-polysemy-eval.jsonl#IP-015` reads the SAME string as the interrogative "how". Eval fixtures are not
  editable in this lane, so the runner records the row as `orthography_undetermined`, asserts the consumer
  fails closed, and claims no identity for it.
- **A declared Boolean clears no mandatory two-vote gate anywhere (rounds 11-12).**
  `tools/fusha_tutor_runtime.py` holds every `two_vote_required` checkpoint as pending — a learner
  `second_check` is recorded as `second_check_declared` and never believed, and the grammar bridge sends no
  `two_vote_done` — and `tools/fusha_placement_test.py` places a learner AT the rung carrying a two-vote
  item rather than past it. A held two-vote rung is a SAFE boundary, not a learner failure: the runner
  under-places rather than clear a grammar-fact gate on a self-report. **Learner placement is not
  certification**: a placement result certifies nothing and promotes no Qurʾānic fact. `ready` is reachable
  only for a bank with no `two_vote_required` item; clearing such an item needs a separately governed
  contract supplying canonical, occurrence-bound external vote artifacts, which does not exist yet.
- **`grammatical_reason` prose is never machine-validated.** The canonical v1.1 contract treats it as
  uncompared elaboration; the authoritative reason gate is the registered reason key plus the complete
  structured conclusion tuple including sign and sign visibility. A caller asking A2 to validate the prose is
  routed to scholar review.

**A2 residual findings (open, packetized):**

- Three `nahw-state-machine-eval.json` rows declared `gate: two_vote_required` while carrying the `ambiguous_grammar` trigger, which the gates SSOT maps to `human_source_review_required`; A2 raised those three gates (a unique, strengthening correction determined by the SSOT). `man_interrogative_fronted`, `anna_how_adverbial`, `ma_relative_vs_negation`.
- `grammar-problems-gates.json#proper_vs_common` declares `two_vote_required` while the SSOT trigger `proper_vs_common_noun` requires `human_source_review_required`. The gate consumer resolves this **upward** at call time (`effective_gate`) and reports it via `python tools/fusha_nahw_gate_rules.py --divergences`; the file is left unedited because the topic↔trigger identification is an owner-level call, not a mechanical one.
- **GAP-N6 — no single "bare label" gate**; pedagogical completeness spread over 3 validators; nothing propagates the two-vote `mood_basis`/governor requirement into public hover payloads. WP-NAHW-BARE-LABEL-GATE.
- **GAP-N7 — particle contract proposed, template deck unenforced**; parity validator runs on a committed fixture only, never live corpus rows. WP-NAHW-PARTICLE-CONTRACT-ADOPT.
- **GAP-N8 — VN full-corpus rerun is manual/external** (5 out-of-repo inputs, `VNMAP-REPORT.md:148`); reproducible only at p007 pilot scale. WP-NAHW-VN-RERUN-HOOK.
- **GAP-N9 — p007 pattern ceiling non-transferable** (per-occurrence host-noun evidence required before the certified template applies).
- **GAP-N10 — clause-relation/exception categories asserted with no DEDICATED analysis fixture.** `nahw/SKILL.md:226-230` vs the deliberately-subset `rel_label` enum. M4: F7 (`hidden-structure-mahall-eval.jsonl`) already drives the `istithna` trigger (the generic `abstained_unmodeled_construction` fallback recognizes إِلَّا), so the gap is a dedicated istithnāʾ CONSTRUCTION handler and edge shape, not zero exercise of the trigger. WP-NAHW-CLAUSE-FIXTURES.
- **GAP-N11 — `nahw/reports/skill-completion-audit-20260624.md` is a stale coverage matrix** (2026-06-24, predates @2.1–@2.4; marks modes "complete" on checklist basis, not test results). WP-NAHW-AUDIT-REGEN.
