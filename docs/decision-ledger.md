# Owner decision ledger — public-safe copy

**Controlling. Do not re-present any decision below as open.** List only residual
implementation/evidence/design-gate work under each.

This is a sanitized derivative of the owner's private ledger
(`DECISION-LEDGER-2026-07-28.md`, reconciled 2026-07-28, extended 2026-07-29).
All server paths, host names, IP addresses, key material/fingerprints and
ops-infrastructure specifics are removed per the public-repo boundary
(AGENTS.md hard rule 3). Where a decision was purely ops-infrastructural its
*outcome* is retained without infrastructure detail. The private ledger is
authoritative where the two differ.

## Architecture decisions (D-1..D-7) — DECIDED

- **D-1** Rich letter-level projection is the target; coarse rows are
  legacy-valid only, never target quality.
  Residual: legacy-vs-rich-projection instrumentation; upgrade waves; doctrine
  fixtures verification.
- **D-2** Genuine source disagreements → attributed-unresolved treatment, no
  false winner. Residual: unresolved-language map must cover
  attributed-disagreement rendering; ملائكة rides this rail (root-silent
  attributed-unresolved, not an open question).
- **D-3** Form-V derivational ت receives its own derivational class (never
  swallowed into STEM, never the imperfect-prefix class).
  Residual: repair the 6 locs (2:259:58 · 4:115:7 · 9:114:12 · 11:56:2 ·
  52:33:3 · 69:44:2) with one worked letter-level render before deploy;
  candidate-mode until a window.
- **D-4** Fused/shared letters → per-case classification; Treatment C where
  ownership is genuinely shared; no whole-word flattening for one hard
  boundary. Residual: revised idghām board (live-style renders,
  source-verified per case); the 10 merge-idghām/subject-suffix held rows
  route here.
- **D-5** Tanwīn is a governed semantic fact; diacritic-only facts may remain
  hover-only; never claim per-diacritic colouring the renderer lacks.
  Residual: ontology entry + hover-fallback contract wording.
- **D-6** Divine-name rows remain root-silent and excluded from automatic
  propagation (the 718 rows stay excluded). Residual: none.
- **D-7** Entry-linkage approved in principle; implementation must consume
  canonical certified projections and satisfy the rich-at-rest +
  learner-language two-surface contracts. Residual: revised entry-linkage
  board → Design Gate → renderer work (not yet authorized).

## Security decisions — DECIDED (outcomes only; infra details private)

- A previously flagged remote-access session was **recognized as
  owner-authorized**; no ANDON, no rotation, no revocation. Do not reopen
  absent new contradictory evidence.
- Additional intrusion-prevention tooling on the server is **NOT AUTHORIZED**;
  do not re-recommend in routine reports. Security ANDON triggers only:
  unrecognized successful login; successful password auth where disabled;
  unauthorized persistence/mutation; unexplained key changes; correlated
  service/firewall/user/cron/systemd changes.

## Programme decisions — DECIDED 2026-07-28

- **Particle plan P-00..P-03: APPROVED** as a versioned candidate-mode
  *organizational* plan (P-00 jarr/conjunction · P-01
  naṣb/jazm/negation/condition · P-02 homograph hard cases · P-03
  pronouns/demonstratives/relatives/remaining). 983/984 is span_live ONLY.
  Membership table: `qamus/data/particle-tranche-membership.json`.
- **Shadow qualification:** subsequently **RETIRED BY OWNER** (2026-07-29):
  the scheduled shadow run was disabled before its first authoritative run;
  Ledger A = historical evidence, Ledger B = owner-cancelled-not-required.
  Future shadow runs need explicit bounded owner authorization.
- **Memory (server service):** de-duplication proof FIRST (non-live); no limit
  change, restart cadence, or storage migration authorized.
- **Ops drift (external site tooling):** controlled harmonization proof,
  separate ops change, never combined with Qamus deployment.
- **@2.1–@2.3 (now @2.1–@2.4) skill rules:** no blanket promotion;
  rule-by-rule adjudication registry; candidate-mode use behind guards
  continues; nothing labeled owner-accepted until adjudicated.
- **Two-vote artifacts must be real:** validators fail on `two_vote` claims
  lacking two reconstructible independent review records (agreement on
  occurrence, segmentation, identity, root/form, function, governor, governed
  expression, case/mood, attachment, referent, grammatical reason, unresolved
  points — not just English gloss). Historical rows: reclassify precisely or
  re-review; never fabricate.
- **Certification authority is fact-level** (per-fact evidence modes, source
  addresses, derivation chains, invalidation rules, reconstructible
  artifacts); never a manual row boolean. See
  `docs/certification-authority.md` + `tools/certify_typed_fact.py`.
- **Server corpora:** no blind bulk commit; custody/reproducibility plan
  required (canonical source, generated-vs-irreplaceable, hashes, minimal
  reproducible subsets, archival, boundary review, DR, storage tiering).
- **Standing boundary:** no live whitelist, renderer, service-limit,
  authentication, or ops mutation authorized.

## Governing frame

The systems re-anchor (meta-transclusive lattice = execution engine of the VN
dogfood flywheel; letter ownership as projection primitive; one artifact → two
surfaces; fact-level certification; compounding-impact accounting per tranche;
terminal-invariant loop) is controlling alongside the architecture doctrine.
Audit-and-extend existing artifacts; never recreate in parallel.

## Scale-out decisions — 2026-07-29 (owner continuation steer)

- **VN-03 NAMESPACE RULING**: authoritative VN-03 = v142–v188 + n0136–n0180 +
  its 3,280-row worklist. The balanced-partition proposal population is NOT
  VN-03 — renamed to `VNPROP-xx`, provenance preserved (applied in PR #127);
  never quote VN-03 counts except against the contract namespace; rerun
  p007→VN effects separately per namespace.
- **p007 completion vocabulary**: the 11 `P007_*` states are the only
  completion language; "p007 accounted" without a named state is forbidden.
  Partition = work plan, not completion: 454 geometry-only (mechanical facts
  ONLY), 866 direct-source (bounded MCP + triangulation per row), 340 two-vote
  (independent artifacts; MCP is never reviewer-B), 2 scholar
  (packets; must not block), 1,337 rejected (defeaters retained as regression
  guards).
- **No deployment**: the 12+454+remap set is NOT one uniformly certified
  payload — separate strata; deployment proposal only when every included
  public fact has required evidence AND the website agent confirms contract
  compatibility.
- **Rule adjudication**: by bounded family as needed; only
  architecture-encoding rules auto-ratified; the owner is never asked to guess
  linguistic answers (scholar/source lanes instead).
- **Next P-family**: بِـ candidate only after its entry id/senses/population
  verified; must reuse p007 machinery and report measured reuse.

## Rate-limit handoff rulings — 2026-07-29 (controlling)

- **Function-certification rung ANSWERED**: an exact, surface-matched source
  statement of حرف جر MAY certify occurrence-level `contextual_function` via
  `direct_source_attestation` (conditions: exact occurrence matched, source
  explicitly states the function, no contradiction, typed fact does not exceed
  the source). It does NOT auto-certify
  governor/governed/attachment/case-sign/scope/sense nuance — those keep their
  own policies.
- **Reason keys**: the 22 new keys register as CANDIDATE vocabulary; promote
  per-key when documented + used by verified artifacts + non-conflating +
  fixtures pass. No blanket promotion.
- **Arbitration/scholar**: genuine disputes stay unresolved and non-blocking;
  the owner is never asked to guess.
- **n225/n912 roots**: direct attestation = candidate root; certify only after
  required corroboration/review; never hold unrelated work.
- **Authority precedence**: (1) verified repo artifacts+tests → (2) owner
  decision ledger → (3) the rate-limit handoff steer+addendum → (4) canonical
  architecture docs → (5) historical charters/dashboards → (6) chat.
  SUPERSEDED markers required for: shadow scheduling/qualification,
  autonomous-live-deploy phrasings, coarse PFX/STEM/SUF as target, simplified
  entry-linkage hovers as standard, proposal-population-as-VN-03.
- **Budget rule**: remaining premium capacity = institutionalization +
  verification + transfer, not content waves. New linguistic examples only to
  validate docs/workflows/edges/cold-start.
- **Visual doctrine**: the referenced "to them" hover = product-direction
  reference, NOT the maximal target; the wooden-lattice sketch is conceptual
  only (never a UI/palette); Kawkab Mono + colour language + rich-at-rest
  locked; font-loading must be deterministically verified in visual tests.
- **Translation**: downstream lattice projection only; never evidence for
  analysis; never English→presumed-Arabic; separate gloss strata;
  divergent-analysis translations both retained.
- **Transfer success**: a cold lower-cost model must demonstrate the 12
  capabilities; the archive must state PROVEN/NOT-PROVEN task classes +
  REQUIRED_ESCALATION_CLASSES; deterministic-tooling competence never implies
  linguistic-adjudication competence.
