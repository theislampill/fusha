# Verbs hover audit (V1)

Pending tokens in verbs-entry āyāt are part of the global pending pool — see `qamus/reports/hover-token-completion.md` (top-500). The V1 sweep authored from that pool by POS; the applied batch is `qamus/candidates/qamus_2092/verb_hover_batch_001.jsonl`. Coverage for verbs entries: 74.98% (77824/103788 tokens). Remaining = `needs_hover_authoring` entries above, by exact pending count.
