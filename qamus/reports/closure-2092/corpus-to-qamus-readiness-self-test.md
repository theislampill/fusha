# Corpus-to-Qamus readiness self-test (closure-2092 Phase 11)

Nawawī40 fixture re-run + validated: **CORPUS FIXTURE OK** — `live_write:false` on every row, no `en`/translation/tafsir field copied, `source_address` all `corpus:*`-scoped, outputs review-only. Candidates link to sarf/nahw procedures via the classification vocabulary. Nawawī40 stays bounded/read-only.

Matn/isnād: the fixture consumes only `{ref, ar}` (matn) — isnād is not ingested; documented requirement for any future hadith corpus.
