# Closure-2092 — baseline (2026-06-24)

Verified from live + repo data, not memory. Companion: `baseline-20260624.json`.

## Git / repo reality

- HEAD `bceb366` = origin/main `bceb366` (clean history).
- Dirty tree: 2 untracked files — `token_irab_batch_002.jsonl` + `.provenance.jsonl`.
- **Finding (commit-hygiene gap):** `token_irab_batch_001` and `_003` are committed but `_002`
  (Phase-5 iʿrāb carryover, +112 tokens, already applied live) was never committed. Provenance
  gap — closed in commit A of this tranche.

## Gates (all run this session, not trusted from prior green)

| gate | result |
|---|---|
| `check_artifact_ergonomics.py` | PASS |
| `check_regressions.py` | PASS |
| `run_grammar_evals.py` | PASS (88 cases) |
| `validate_qamus_completion_manifest.py` | PASS (49,900 terminal) |
| `validate_entry_completion_rollup.py` | PASS (2,092 terminal) |
| `validate_sarf_skill.py` | PASS |
| `validate_nahw_skill.py` | PASS |

## Live hover state (staged from production artifact)

| metric | value |
|---|---:|
| health (HTTP) | 200 |
| coverage | 85.02% |
| resolved | 42,424 |
| total tokens | 49,900 |
| pending | 7,476 |
| token decisions | 2,164 |
| public entry count (read-only) | 2,092 |

## Coarse blockers

| blocker | count |
|---|---:|
| `stem_base_unknown` | 5,993 |
| `source_entry_unverified` | 1,103 |
| `same_surface_polysemy_requires_i3rab` | 379 |
| `proper_noun_no_qamus_entry` | 1 |

## Root-cause decomposition (Phase 3 ledger)

`stem_base_unknown` (5,993):

| root cause | tokens |
|---|---:|
| `missing_form_variant_on_existing_entry` | 3,859 |
| `host_lexeme_possessive_candidate` | 1,210 |
| `missing_qamus_entry_candidate` | 473 |
| `particle_or_pronoun_misclassified_as_stem` | 418 |
| `nominal_derivative_candidate` | 14 |
| `truly_low_frequency_tail` | 11 |
| `source_entry_required_before_resolution` | 8 |

`source_entry_unverified` (1,103): `forms_array_missing_surface` 657 · `quran_refs_missing_or_incomplete` 397 · `source_photo_visual_needed` 49.

`same_surface_polysemy_requires_i3rab` (379): `genuinely_ambiguous_pending` 148 · `verb_form_or_voice` 148 · `content_homograph` 82 · `particle_function` 1.

Form-variant safety tiers: `A_safe` 73 · `B_sense_select` 1,679 · `C_pos_or_collision` 2,764.

## Entries / graph

- Entries: 2,092 (947 verb / 1,045 noun / 100 particle) — confirmed from `entries.jsonl` and live site.
- Source-address graph: 28,393 addresses (`source_sha=65797d7d5599fadd`), integrity gate green.

## Frontier correction (supersedes the prior tranche)

The prior tranche concluded "safe frontier ~86%, 90% not safely reachable." That measured **only
surface-wide auto-glossing** — one mechanism. Because QAC assigns a root to every pending token,
the dominant remaining lever is **root-known structured authoring** (pick POS, select sense, guard
same-root/different-lexeme collisions; 2-vote certified). The yield-ledger v2 estimates 3,684
safe-realizable tokens → **92.40% ceiling**, so **90% IS reachable** as bounded multi-batch authoring.
It is NOT a free `add_form` (e.g. ظَمَأٌ noun vs verbal entry "to be thirsty"; بَنِين "sons" vs root
ب ن ي "to build"), and the genuine-ambiguity residue is only ~148 tokens.

## Next root-cause queues (ranked, Phase 4 yield ledger)

1. `missing_form_variant_on_existing_entry` (3,859) — author POS-correct form gloss, 2-vote.
2. `host_lexeme_possessive_candidate` (1,210) — host stem + possessive compose.
3. `forms_array_missing_surface` (657) — entry-repair form gloss.
4. `missing_qamus_entry_candidate` (473) — owner-gated new-entry proposals.
5. `particle_or_pronoun_misclassified_as_stem` (418) — function/token decision.
6. `quran_refs_missing_or_incomplete` (397) — entry completeness.
7. `same_surface_polysemy` remaining (378) — per-loc iʿrāb.
